{
  "name": "@autoexpect/mcp-sseclient-browser",
  "version": "1.1.4",
  "description": "A browser-optimized SSEClient module for managing Server-Sent Events.",
  "main": "dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"No tests specified\" && exit 0",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "openai": "^4.96.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "patch-package": "^8.0.0",
    "typescript": "^4.0.0"
  },
  "browserslist": [
    "last 2 versions",
    "not dead"
  ],
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0"
  }
}